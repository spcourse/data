grades:
    project:
        subgrades:
            process: float
            code: float
            product: float
        calculation: (.2 * process + .3 * code + .5 * product).round(1)

calculation:
    final:
        project: 100

project:
    submits:
        project: 1
